/* exported getFirstChar */
// declare a new function named 'getFirstChar' with one parameter : 'string'
// access the first character of the string and return it

function getFirstChar(string) {
  // return the first character of string
  return string[0];
}
