/* exported getLastChar */
// declare a function named 'getLastChar' with one parameter: 'string'
// return the last character of string by using [string.length-1]

function getLastChar(string) {
  // get last character of the string
  return string[string.length - 1];
}
