/* exported getValue */
// declare a new function named 'getValue' with 2 parameters: object and key
// return the value of object[key]
function getValue(object, key) {
  // return the value of object[key]
  return object[key];
}
