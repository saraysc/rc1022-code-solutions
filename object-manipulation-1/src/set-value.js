/* exported setValue */
// declare a new function named 'setValue' with 3 parameters: object, key and value
// assign the value into the object[key];
// return the object

function setValue(object, key, value) {
  // assign the value into the object[key];
  object[key] = value;
  // return the object
  return object;
}
